extends layout

block pane
  div(id="pane")
    h1= title
    p Welcome to #{title}
    form(action="/" method="POST")
      input(type="text")
      input(type="submit" value="submit")

block map
  div(id="map")